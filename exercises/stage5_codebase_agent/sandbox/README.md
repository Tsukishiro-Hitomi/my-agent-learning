# 订单服务 order-service

一个示例微服务，负责接收和查询订单。**仅用于阶段 5 练习**——你的 agent 会来探索它。

## 目录结构
- `app/`      业务代码（服务入口 + 请求处理器）
- `config/`   配置（数据库连接、部署参数等）
- `utils/`    通用工具函数

## 启动方式
读取 `config/` 里的配置后启动 HTTP 服务。具体监听参数、进程数等都在配置里，不在这份 README 里。
